tool
extends ImmediateGeometry

var contour = ImmediateGeometry.new()

func _ready():
	pass

#func _process(delta):
#	contour.clear()
#	contour.begin(Mesh.PRIMITIVE_LINE_LOOP)
#	contour.set_color(ColorN("Red"))
#	for i in range(39):
#		contour.add_vertex(Vector3(i,1,i))
#	contour.end()
